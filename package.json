{
  "name": "ask-it",
  "private": true,
  "dependencies": {
    "@popperjs/core": "^2.11.2",
    "@rails/actioncable": "^6.0.0",
    "@rails/activestorage": "^6.0.0",
    "@rails/ujs": "^6.0.0",
    "bootstrap": "^5.1.3",
    "esbuild": "^0.14.11",
    "sass": "^1.48.0",
    "tom-select": "^2.0.0",
    "turbolinks": "^5.2.0",
    "postcss-cli": "^9.1.0",
    "postcss": "^8.4.5",
    "autoprefixer": "^10.4.2"
  },
  "browserslist": [
    "defaults"
  ],
  "version": "0.1.0",
  "scripts": {
    "build:css:dev": "sass --style compressed ./app/assets/stylesheets/application.bootstrap.scss ./app/assets/builds/application.css --no-source-map --load-path=node_modules",
    "build:css": "yarn build:css:dev && postcss ./app/assets/builds/application.css --replace --use autoprefixer --no-map",
    "build": "esbuild app/javascript/application.js --bundle --minify --outdir=app/assets/builds"
  }
}
